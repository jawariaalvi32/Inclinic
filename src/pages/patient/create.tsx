import * as React from 'react';
import { CreateOnModalProps } from 'src/types/ComponentTypes';

const CreatePatient = ({ isOpen, openModal, data }: CreateOnModalProps) => {

    return (
        <div>

        </div>
    )
}

export default CreatePatient
